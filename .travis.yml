language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t manjukris/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true manjukris/docker-react npm run test -- --coverage